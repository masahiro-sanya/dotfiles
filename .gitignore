# Secrets / credentials
*.pem
*.key
*credentials*
*secret*
*token*

# SSH private keys
ssh/id_*
!ssh/*.pub

# setup.sh のバックアップ（保存先はリポ外に移したが、旧版が作った backup/ を追跡しない保険）
backup/

# Claude Code
claude/settings.local.json
claude/claude.json
claude/projects/
claude/statsig/
claude/stats-cache.json
claude/todos/
claude/shell-snapshots/
claude/cache/
claude/logs/
claude/debug/
claude/sessions/
claude/history.jsonl
claude/file-history/
claude/telemetry/
claude/ide/
claude/paste-cache/
claude/backups/
claude/session-env/
claude/tasks/

# Karabiner-Elements (設定変更のたびに自動バックアップが生成される)
karabiner/automatic_backups/

# AWS / GCP
aws/credentials
aws/sso/
gcloud/

# Docker
docker/

# GitHub CLI tokens
gh/hosts.yml

# macOS
.DS_Store
*.swp
*.swo
*~

# Editor workspace state
editor/vscode/workspaceStorage/

# Project-local Claude Code settings
.claude/
